-- ============================================================
-- 002_add_sort_order.sql - 变更记录 (v1.1)
-- 日期: 2026-04-09
-- 变更: 为4张表添加 sort_order 字段，支持拖拽排序功能
-- 前置: 需先执行 001_init.sql
-- ============================================================

USE mydata;

-- -----------------------------------------------------------
-- 项目表：添加排序字段
-- -----------------------------------------------------------
ALTER TABLE `projects`
    ADD COLUMN `sort_order` INT DEFAULT 0 COMMENT '排序序号'
    AFTER `description`;

-- -----------------------------------------------------------
-- API分组表：添加排序字段
-- -----------------------------------------------------------
ALTER TABLE `api_groups`
    ADD COLUMN `sort_order` INT DEFAULT 0 COMMENT '排序序号'
    AFTER `description`;

-- -----------------------------------------------------------
-- API配置表：添加排序字段
-- -----------------------------------------------------------
ALTER TABLE `api_configs`
    ADD COLUMN `sort_order` INT DEFAULT 0 COMMENT '排序序号'
    AFTER `description`;

-- -----------------------------------------------------------
-- 环境配置表：添加排序字段
-- -----------------------------------------------------------
ALTER TABLE `environments`
    ADD COLUMN `sort_order` INT DEFAULT 0 COMMENT '排序序号'
    AFTER `description`;
